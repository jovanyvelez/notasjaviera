<script>
// Este componente solo muestra ejercicios, no requiere lógica JS
</script>

<main class="ejercicios38-container">
  <h1>📝 38 Ejercicios de Ciclos y Arreglos en C#</h1>
  <p class="intro">¡Parceros y parceras! Aquí tienen 38 ejercicios para practicar lo aprendido sobre ciclos (<code>for</code>, <code>while</code>, <code>foreach</code>) y arreglos en C#. Cada uno está pensado para que lo resuelva un estudiante diferente, pero todos tienen el mismo nivel de dificultad. ¡A programar se dijo!</p>
  <ol class="ejercicios-lista">
    <li>Crea un arreglo con los nombres de 5 frutas y usa un ciclo <code>for</code> para imprimir cada fruta.</li>
    <li>Usa un ciclo <code>while</code> para contar de 1 a 10 y mostrar cada número en consola.</li>
    <li>Crea un arreglo con 4 colores y usa <code>foreach</code> para mostrar cada color.</li>
    <li>Haz un ciclo <code>for</code> que imprima los números pares del 2 al 20.</li>
    <li>Crea un arreglo con 6 nombres de amigos y usa <code>for</code> para saludarlos uno a uno.</li>
    <li>Usa <code>while</code> para sumar los números del 1 al 5 y mostrar el resultado final.</li>
    <li>Crea un arreglo con 3 comidas favoritas y usa <code>foreach</code> para imprimirlas.</li>
    <li>Haz un ciclo <code>for</code> que cuente hacia atrás del 10 al 1.</li>
    <li>Crea un arreglo con 5 números y usa <code>for</code> para calcular la suma total.</li>
    <li>Usa <code>while</code> para pedir al usuario números hasta que escriba 0, y muestra cuántos números ingresó.</li>
    <li>Crea un arreglo con 4 animales y usa <code>foreach</code> para mostrar cada uno con un mensaje divertido.</li>
    <li>Haz un ciclo <code>for</code> que imprima la tabla del 3 (del 3 al 30).</li>
    <li>Crea un arreglo con 7 días de la semana y usa <code>for</code> para imprimirlos.</li>
    <li>Usa <code>while</code> para restar de 20 en 2 hasta llegar a 0.</li>
    <li>Crea un arreglo con 5 ciudades de Colombia y usa <code>foreach</code> para saludarlas.</li>
    <li>Haz un ciclo <code>for</code> que imprima los números impares del 1 al 15.</li>
    <li>Crea un arreglo con 6 notas y usa <code>for</code> para calcular el promedio.</li>
    <li>Usa <code>while</code> para sumar números ingresados por el usuario hasta que la suma supere 100.</li>
    <li>Crea un arreglo con 4 deportes y usa <code>foreach</code> para animar a practicarlos.</li>
    <li>Haz un ciclo <code>for</code> que imprima los múltiplos de 5 del 5 al 50.</li>
    <li>Crea un arreglo con 5 nombres y usa <code>for</code> para mostrar el nombre y su cantidad de letras.</li>
    <li>Usa <code>while</code> para pedir contraseñas hasta que el usuario escriba "secreto".</li>
    <li>Crea un arreglo con 3 películas favoritas y usa <code>foreach</code> para recomendarlas.</li>
    <li>Haz un ciclo <code>for</code> que imprima los cuadrados de los números del 1 al 8.</li>
    <li>Crea un arreglo con 5 precios y usa <code>for</code> para calcular el total a pagar.</li>
    <li>Usa <code>while</code> para pedir edades hasta que el usuario escriba una edad menor que 0, y muestra el promedio.</li>
    <li>Crea un arreglo con 4 instrumentos musicales y usa <code>foreach</code> para decir cuál te gustaría aprender.</li>
    <li>Haz un ciclo <code>for</code> que imprima la suma acumulada del 1 al 10.</li>
    <li>Crea un arreglo con 6 palabras y usa <code>for</code> para mostrar solo las que tienen más de 5 letras.</li>
    <li>Usa <code>while</code> para pedir números hasta que el usuario escriba un número negativo, y muestra la cantidad de positivos.</li>
    <li>Crea un arreglo con 5 canciones y usa <code>foreach</code> para crear una playlist en consola.</li>
    <li>Haz un ciclo <code>for</code> que imprima los números del 100 al 90 (descendente).</li>
    <li>Crea un arreglo con 4 materias y usa <code>for</code> para pedir y mostrar la nota de cada una.</li>
    <li>Usa <code>while</code> para pedir nombres hasta que el usuario escriba "fin", y muestra todos los nombres ingresados.</li>
    <li>Crea un arreglo con 3 marcas de ropa y usa <code>foreach</code> para decir cuál prefieres.</li>
    <li>Haz un ciclo <code>for</code> que imprima la secuencia: 2, 4, 8, 16, 32, 64.</li>
    <li>Crea un arreglo con 5 apellidos y usa <code>for</code> para mostrarlos en orden inverso.</li>
    <li>Usa <code>while</code> para pedir números hasta que la suma sea exactamente 50, y muestra cuántos intentos tomó.</li>
  </ol>
</main>

<style>
.ejercicios38-container {
  max-width: 800px;
  margin: 2rem auto;
  padding: 2rem 1rem;
  background: #f8fafc;
  border-radius: 1.5rem;
  box-shadow: 0 4px 24px 0 #0001;
  font-family: 'Segoe UI', Arial, sans-serif;
}
.ejercicios38-container h1 {
  color: #0e7490;
  font-size: 2rem;
  margin-bottom: 1rem;
  text-align: center;
}
.ejercicios38-container .intro {
  font-size: 1.1rem;
  margin-bottom: 1.5rem;
  text-align: center;
}
.ejercicios-lista {
  margin-left: 1.5rem;
  font-size: 1.05rem;
}
.ejercicios-lista li {
  margin-bottom: 0.7rem;
  background: #e0f2fe;
  border-left: 5px solid #0ea5e9;
  border-radius: 0.5rem;
  padding: 0.7rem 1rem;
  list-style-position: inside;
}
@media (max-width: 600px) {
  .ejercicios38-container {
    padding: 0.7rem 0.1rem;
    border-radius: 0.7rem;
  }
  .ejercicios38-container h1 {
    font-size: 1.2rem;
  }
  .ejercicios-lista {
    font-size: 0.97rem;
  }
}
</style>
